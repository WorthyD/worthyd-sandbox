import { ColorDisplayPipe } from './color-display.pipe';

describe('ColorDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
